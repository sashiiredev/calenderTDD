
import {Event} from "../../src/entities/Event";

describe('class Event', () => {

    let instance: Event;

    beforeEach(() => {
        instance = new Event("", undefined, undefined, "", "", undefined);
    });
})
